# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# test artifacts
playwright-report/
test-results/
blob-report/
coverage/
# Stryker mutation testing outputs — regenerated on every `pnpm mutate`.
.stryker-tmp/
reports/mutation/
# Playwright storageState produced by the setup project — regenerated each run.
tests/.auth/

# agent-generated test drafts (review & promote, don't commit)
tests/e2e/_generated/*.draft.spec.ts

# env / secrets
.env
.env.*
!.env.example

# prisma
packages/db/prisma/*.db

# logs / os
*.log
.DS_Store
Thumbs.db
