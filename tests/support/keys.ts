// localStorage keys the storefront uses for auth state. Mirrored from
// `apps/web/lib/auth.tsx` — kept here so the `authedPage` fixture can inject
// state without importing from the Next app.
export const TOKEN_KEY = 'qa_token';
export const USER_KEY = 'qa_user';
